============
Data Sets
============

Currently the PyTorch version of DeepOBS includes six different data sets. Each data set inherits from
the same base class with the following signature: ``deepobs.pytorch.datasets.dataset.Dataset``


